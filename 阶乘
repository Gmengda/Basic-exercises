#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main()
{
	int i, j;
	int n;
	int sum = 0;
	int s = 1;
	scanf("%d", &n);
	for (i = 1; i <= n; i++)
	{
		s = 1;
		for (j = 1; j <= i; j++)
		{
			s *= j;
		}
		sum += s;
	}
	printf("%d", sum);
	return 0;
}
