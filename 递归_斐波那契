#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int Fibonacci(int n)
{
	if (n<=2)
	{
		return 1;
	}
	else
	{
		return Fibonacci(n - 1) + Fibonacci(n - 2);
	}
}

int main()
{
	int n = 0;
	scanf("%d", &n);
	int c = Fibonacci(n);
	printf("%d", c);
	return 0;
}
